@model CourseIndexViewModel
@{
    ViewData["Title"] = "Danh Mục Khóa Học";
    var currentTheme = Model.Theme;
}

<div class="container py-4 course-page">
    <div class="row align-items-center mb-4">
        <div class="col-md-7">
            <h2 class="fw-extrabold m-0" style="letter-spacing: -1px;">Danh Mục Khóa Học</h2>
            <p class="text-muted m-0 mt-1">Hệ thống quản lý thông tin giảng dạy và điều phối sĩ số lớp học trực quan.</p>
        </div>
        <div class="col-md-5 text-md-end mt-3 mt-md-0">
            <a class="btn app-btn app-btn-primary shadow-sm fw-bold px-4 py-2" asp-controller="Courses" asp-action="Stats" asp-route-theme="@currentTheme"><i class="bi bi-bar-chart-fill me-2"></i>Báo Cáo</a>
        </div>
    </div>


    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-3">
        <div><h5 class="fw-bold mb-1">Danh sách lớp học vận hành</h5><p class="text-muted mb-0">Mỗi khóa học được hiển thị bằng Course Card từ Partial View.</p></div>
        <span class="badge bg-primary-subtle text-primary rounded-pill px-3 py-2 fw-bold">@Model.Courses.Count/@Model.TotalCoursesBeforeFilter lớp</span>
    </div>

    @if (Model.Courses.Count == 0)
    {
        <div class="empty-state text-center p-5 rounded-4"><div class="display-6 fw-bold mb-2">Không tìm thấy khóa học</div><p class="text-muted mb-4">Thử đổi từ khóa hoặc chọn lại chuyên ngành để xem dữ liệu phù hợp hơn.</p><a asp-controller="Courses" asp-action="Index" asp-route-theme="@currentTheme" class="btn app-btn app-btn-primary fw-bold">Xem toàn bộ khóa học</a></div>
    }
    else
    {
        <div class="row g-4">
            @foreach (var item in Model.Courses)
            {
                <div class="col-md-6 col-xl-4"><partial name="~/Views/Shared/_CourseCard.cshtml" model="item" /></div>
            }
        </div>
    }
</div>
